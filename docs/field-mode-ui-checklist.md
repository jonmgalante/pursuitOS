# Field Mode UI Checklist

Use this quick manual pass after field-mode UI changes:

1. Open [`/workspaces/ws_demo_summit_2026/field`](/Users/jongalante/Desktop/pursuitOS/apps/web/app/workspaces/[workspaceId]/field/page.tsx) after seeding and capture.
2. Select a person from quick picks or the match list and confirm the selected-person panel updates.
3. Enter a short note in the note field.
4. Toggle the outcome between `Met` and `Missed` and confirm the active state is obvious.
5. Tap one or more tags and confirm they stay visibly selected.
6. Optionally attach session and speaker context.
7. Use `Save and stay in field mode` and confirm the save succeeds, the UI stays on field mode, and recent activity reflects the new note.
